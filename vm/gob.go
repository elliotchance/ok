package vm

import "encoding/gob"

func init() {
	gob.Register(&Add{})
	gob.Register(&And{})
	gob.Register(&Append{})
	gob.Register(&ArrayAlloc{})
	gob.Register(&ArrayGet{})
	gob.Register(&ArraySet{})
	gob.Register(&Assert{})
	gob.Register(&Assign{})
	gob.Register(&Call{})
	gob.Register(&CastChar{})
	gob.Register(&CastNumber{})
	gob.Register(&CastString{})
	gob.Register(&Combine{})
	gob.Register(&Concat{})
	gob.Register(&Divide{})
	gob.Register(&DynamicCall{})
	gob.Register(&Equal{})
	gob.Register(&EqualNumber{})
	gob.Register(&Finally{})
	gob.Register(&Get{})
	gob.Register(&GreaterThanEqualNumber{})
	gob.Register(&GreaterThanEqualString{})
	gob.Register(&GreaterThanNumber{})
	gob.Register(&GreaterThanString{})
	gob.Register(&Interface{})
	gob.Register(&Interpolate{})
	gob.Register(&Jump{})
	gob.Register(&JumpUnless{})
	gob.Register(&Len{})
	gob.Register(&LessThanEqualNumber{})
	gob.Register(&LessThanEqualString{})
	gob.Register(&LessThanNumber{})
	gob.Register(&LessThanString{})
	gob.Register(&Log{})
	gob.Register(&MapAlloc{})
	gob.Register(&MapGet{})
	gob.Register(&MapSet{})
	gob.Register(&Multiply{})
	gob.Register(&NextArray{})
	gob.Register(&NextMap{})
	gob.Register(&NextString{})
	gob.Register(&Not{})
	gob.Register(&NotEqual{})
	gob.Register(&NotEqualNumber{})
	gob.Register(&On{})
	gob.Register(&Or{})
	gob.Register(&ParentScope{})
	gob.Register(&Power{})
	gob.Register(&Print{})
	gob.Register(&Props{})
	gob.Register(&Raise{})
	gob.Register(&Remainder{})
	gob.Register(&Return{})
	gob.Register(&Set{})
	gob.Register(&StringIndex{})
	gob.Register(&Subtract{})
	gob.Register(&Type{})
}
