// Reverse creates a string with all characters in the opposite order.
func Reverse(s string) string {
    result = ""
    for i = len(s) - 1; i >= 0; --i {
        result += string(s[i])
    }

    return result
}
