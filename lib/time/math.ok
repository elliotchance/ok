// TODO(elliot): Remove this once standard libraries can import each other.
func floor(x number) number {
    frac = x % 1
    if frac == 0 {
        return x
    }

    if x < 0 {
        return x - frac + 1
    }

    return x - frac
}
